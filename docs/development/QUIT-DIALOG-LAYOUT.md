# Exit confirmation text clipping

Work branch: `fix/quit-dialog-layout`, from complete panel checkpoint `d892374c`,
including the separate shadow correction. The shared checkout remains on the
parallel shadow branch; no previous fork changes are discarded.

The user's 13:21:17 screenshot shows the exit confirmation title and replay
checkbox label cut off. `gui/WindowQuit.layout` fixes the dialog width to 400
design pixels and the checkbox to 182. The existing skin reserves 75 pixels
from the title width and the checkbox image plus padding from its label width.
The displayed font therefore needs more space than these rectangles provide.
The accepted Escape behavior and exit/replay callbacks already work.

Measure the actual label extents and existing scaled geometry before adjusting
only these two horizontal layout areas; preserve text, buttons, replay state,
confirmation behavior and the shared skin. Verify the five supported viewport
sizes through 80/100/120/100% scaling with actual CEGUI fonts and hit targets.

## Correction and verification

Only two layout areas changed: the centered dialog is now 540 design pixels wide,
and the centered replay checkbox is 240 wide. Existing vertical positions, text,
button dimensions, skin and callbacks are preserved. At 800x600 and 80% scaling,
the title needs 252 pixels and previously had 175; it now has 263. At 100%, the
replay label needs 164 pixels and previously had 128; it now has 174.

The isolated CEGUI probe checks actual font extents against the loaded skin's
text-area geometry, close-button clearance, viewport bounds, all four control hit
targets and replay-checkbox toggling. Across five viewport sizes and
80/100/120/100% changes, 60 of 260 checks failed before; all 260 pass afterward.
Logs: `build/windows/quit-dialog-before-results.log` and
`quit-dialog-probe-results.log`. The command is
`powershell.exe -NoProfile -ExecutionPolicy Bypass -File build/windows/build-quit-dialog-probe.ps1`.

An isolated real Ogre/CEGUI render also shows the full title and replay text:
`build/reference-audit/quit-dialog-current.png`, generated by
`build/windows/build-quit-dialog-render-preview.ps1`. It loads the current skin,
fonts and game layout without starting a game or executing quit/save operations.
The assistant inspected that rendered image; user gameplay acceptance is pending.

No binary rebuild is needed for this XML-only correction: the existing
`build/windows/gui` junction points directly to the repository's `gui` directory.
The prepared 16:39:18 executable loads the corrected layout on its next start.
Version remains 0.7.1; no release, changed command or README instruction is needed,
and no changelog exists. The focused change is documented here and linked from
the development index; no push or upstream PR was made.
